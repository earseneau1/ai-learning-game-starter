import type { Ending, GameDefinition, PlayState } from '../types/game';

export function EndingScreen({ game, state, ending, onRestart }: { game: GameDefinition; state: PlayState; ending: Ending; onRestart: () => void }) {
  const evidence = game.inventory.filter((item) => state.inventory.includes(item.id));
  return <main className={`ending-page ending-${ending.tone}`}><section className="ending-hero"><p className="eyebrow">Your Startup Sprint ending</p><h1>{ending.title}</h1><p>{ending.narrative}</p></section><div className="debrief-grid"><section><h2>How you got here</h2><p>{ending.explanation}</p><ol className="decision-trail">{state.decisions.map((decision) => <li key={`${decision.sceneId}-${decision.choiceId}`}><strong>{decision.choiceLabel}</strong><span>{decision.consequence}</span></li>)}</ol></section><aside><h2>Final snapshot</h2>{game.variables.map((variable) => <div className="final-variable" key={variable.id}><span>{variable.icon} {variable.label}</span><strong>{state.variables[variable.id]}</strong></div>)}<h3>Strongest evidence</h3><p>{evidence.length ? evidence.map((item) => item.name).join(' · ') : 'No behavioral evidence artifact was collected.'}</p></aside></div><section className="debrief"><p className="eyebrow">Learning debrief</p><h2>What this path illustrates</h2><p>{ending.learningDebrief}</p><ul>{ending.principles.map((principle) => <li key={principle}>{principle}</li>)}</ul><div className="reflection"><strong>Reflect</strong><p>{ending.reflectionQuestion}</p></div><p className="replay-note"><strong>Try another path:</strong> {ending.replaySuggestion}</p><button className="primary-button" onClick={onRestart}>Play Startup Sprint again</button></section></main>;
}
